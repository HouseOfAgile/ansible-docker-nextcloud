APP_NAME={{ item.name }}
APP_MAIN_DOMAIN={{ item.app_main_domains|join(' ') }}
APP_TRAEFIK_DOMAINS={{ item.app_traefik_domains}}
NEXTCLOUD_VOLUME_SIZE={{ item.nextcloud_volume_size }}
MYSQL_ROOT_PASSWORD={{ item.mysql_root_password }}
MYSQL_PASSWORD={{ item.mysql_password }}
MYSQL_DATABASE={{ item.mysql_database }}
MYSQL_USER={{ item.mysql_user }}
NEXTCLOUD_ADMIN_USER={{ item.nextcloud_admin_user }}
NEXTCLOUD_VERSION={{ item.nextcloud_image_version }}
NEXTCLOUD_ADMIN_PASSWORD={{ item.nextcloud_admin_password }}
SMTP_HOST={{ item.smtp_host }}
SMTP_SECURE={{ item.smtp_secure }}
SMTP_PORT={{ item.smtp_port }}
SMTP_NAME={{ item.smtp_name }}
SMTP_PASSWORD={{ item.smtp_password }}
